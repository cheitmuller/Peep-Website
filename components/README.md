# Components Directory

This directory contains all React components for the Peep website.

## Structure

- `/layout` - Layout components (Header, Footer, etc.)
- `/shared` - Reusable shared components (buttons, cards, etc.)
- `/features` - Feature-specific components
